# Books Directory

Place your target books (in `.pdf` or `.docx` format) here for extraction and skill conversion.

Supported formats:
- PDF (`.pdf`)
- DOCX (`.docx`)

> **Note**: Book files added here are excluded from git tracking via `.gitignore` to prevent copyright issues.
